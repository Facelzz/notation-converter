/**
*	Индивидуальное задание
*	по АВС
**/

#include <iostream>

// bool checker(int, int);

// int toDecimal(int, int)

// void toBinary(int*, int, bool*)

// bool *summ(bool*, bool*) or void add(bool*, bool*, bool*)

// void printBinary(bool*);

int main() {
	int X = 0, Y = 0;
	bool binariX[12], binaryY[12], binaryResult[12];
	int _X = 0, _Y = 0;

	return 0;
}
